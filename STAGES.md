# ステージ編集ガイド

キャンペーンの正本は `stages.json` です。ゲームは起動時にこのファイルを読み込みます。キャラクター性能と排便演出は従来どおり `studio.html`、ステージと難易度は `stages.html` で編集します。

## いちばん簡単な編集方法

1. ローカルHTTPサーバーで `stages.html` を開く。
2. 左側のフォームでステージまたは難易度を変更する。
3. 「JSONへ反映」→「JSONを検証」を押す。
4. 「stages.jsonを書き出す」でダウンロードし、リポジトリの `stages.json` と差し替える。
5. `tactics.html` を再読み込みして確認する。

ブラウザの制約により、エディタからリポジトリのファイルを直接上書きはしません。書き出したJSONをGitでレビューできるため、複数人での開発でも変更理由と差分が残ります。

## ステージの主な項目

```json
{
  "id": "1-1",
  "chapter": 1,
  "name": { "ja": "苔の谷", "en": "Moss Hollow" },
  "rounds": 4,
  "terrain": { "seed": 101, "hills": 1, "hillRadius": 2, "river": "none", "groveTrees": 3 },
  "objective": { "type": "survive" },
  "waves": [
    { "round": 2, "kind": "killer", "count": 1 }
  ]
}
```

- `rounds`: ステージの長さ。序盤は4〜8程度から始める。
- `terrain`: `"inherit"` なら前ステージの森・高低差をそのまま引き継ぐ。オブジェクトなら新しい地形を生成する。
- `seed`: 同じ数値なら同じ地形になる。ゲーム結果に関わるランダム生成は使わない。
- `river`: `none` / `narrow` / `wide`。
- `objective.type`: `survive`（指定ラウンド生存）/ `wipe`（全増援を倒す）/ `forest`（森の規模）/ `births`（そのステージ中の誕生数）。`forest` と `births` は `target` も指定する。
- `waves`: 敵の出現ラウンド、種類、基本数。敵IDは `killer` / `bald` / `fireman` / `jikon`。
- `enemyScale`: そのステージだけの敵倍率。例: `{ "hp": 1.2, "atk": 1.1 }`。
- `reinforce`: 森が大きい場合の追加敵。例: `{ "perForest": 25, "max": 2 }`。
- `fireSpread`: そのステージだけの延焼倍率。
- `camp`: `false` にすると報酬選択を挟まず次へ進む。

## 難易度

- `enemyHp` / `enemyAtk`: 全ステージの敵能力倍率。
- `waveCount`: 各ウェーブの敵数倍率。端数は四捨五入し、最低1体。
- `roundsScale`: 全ステージのラウンド倍率。
- `stageScale.hp` / `stageScale.atk`: 1ステージ進むごとの敵成長率。
- `fireSpread`: 延焼倍率。
- `reinforceMax`: 森の規模で増える追加敵の上限。
- `birthTreeTurns`: 出生に必要な成木の生存ターン。
- `maxApes`: 味方上限。
- `allyBonus`: 開始時の味方補正。

難易度を切り替えると、選択値はブラウザに保存され、再読み込み後に適用されます。難易度ごとにセーブデータを分離判定するため、別難易度の途中データを誤って読み込むことはありません。

## 現在のキャンペーン設計

- 全6章・24ステージ、想定30時間。
- 序盤は4、5、6、8ラウンドで、食べる・排便・出生を順に覚える。
- 章の途中は森を引き継ぎ、章の開始時だけ新しい地形に切り替える。
- 後半は伐採、火、ゴリラジコン、森規模・誕生・全滅の目標を段階的に追加する。

数値を変えたら `node smoke-test.mjs` でもJSON全体を検証してください。

## キャラクターを追加する

1. `characters.html` を開く。
2. 味方または敵と、近い既存キャラクターをテンプレートに選ぶ。
3. 新しいID・名前・能力値・出生色を入力し、「キャラクターを追加」を押す。
4. 検証後に `characters.json` を書き出し、リポジトリの同名ファイルと差し替える。
5. 敵なら `stages.html` の襲撃に新IDを指定する。味方なら開始編成または出生候補として利用できる。

`sprite`、味方の `skill.behavior`、敵の `ai` は既存キャラクターから再利用できます。そのため、能力違い・上位種はJSONだけで追加できます。完全に新しいスキル挙動やAIが必要な場合だけ `tactics-engine.js` にテンプレートを1つ追加してください。

## スプライトを編集・追加する

1. `sprites.html` を開き、既存キャラクターを選ぶ。
2. 鉛筆・消しゴム・色取得で編集し、「この絵をJSONへ保存」を押す。
3. 新キャラクター用なら、別のスプライトIDへ複製する。
4. 検証後に `sprites.json` を書き出して、リポジトリの同名ファイルと差し替える。
5. `characters.html` の `sprite` に、そのスプライトIDを指定する。

未編集の絵は従来の内蔵スプライトを使用し、編集済みの絵だけを `sprites.json` で上書きします。1枚は最大64×64ピクセル、16色までです。上書きを削除すれば内蔵スプライトへ戻ります。
